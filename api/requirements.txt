fastapi>=0.115.5
uvicorn[standard]>=0.32.1
pydantic>=2.10.3
sse-starlette>=2.1.3
python-dotenv>=1.0.1
google-genai>=0.8.0
firebase-admin>=6.5.0
# TUI alternate interface (api/tui.py) — optional for prod deployment but kept
# in the main requirements file so `pip install -r requirements.txt` gives a
# working `python -m api.tui` against a running uvicorn server.
# Verified latest stable Textual 8.x via Context7 textualize/textual; httpx is
# already a transitive dep of google-genai but pinned here so the TUI's SSE
# code has a stable async-streaming surface.
textual>=0.80
httpx>=0.27
